// --------------------------------------------------------------------------------------
var $nome, $apelido, $nome_completo, $email, $senha_1, $senha_2, $país, $dia, $mês, $ano, $sexo;

let lista_nomes, lista_apelidos, lista_nomes_completo, lista_email, lista_senha_1, lista_senha_2, lista_país;



// -------------------------------------------------------------------------------------------

// -------------------------------------------------------------------------------------------

function coletar() {

    $nome = String(window.document.getElementById("input-nome").value);
    $apelido = String(window.document.getElementById("input-apelido").value);
    $nome_completo = String(window.document.getElementById("input-nome-completo").value);
    $email = String(window.document.getElementById("input-email").value);
    $senha_1 = String(window.document.getElementById("input-senha").value);
    $senha_2 = String(window.document.getElementById("input-confirmar-senha").value);
    $país = String(window.document.getElementById("select-pais").value);
    $dia = String(window.document.getElementById("data").value);
    $mês = String(window.document.getElementById("mes").value);
    $ano = String(window.document.getElementById("ano").value);
    $sexo = String(window.document.getElementById("sexo").value);

    // ----------------------------------------

    
}